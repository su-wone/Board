@AGENTS.md

# 📄 CLAUDE.md (공통)

이 문서는 Claude(Code)가 이 저장소에서 코드를 읽고, 수정하고, 제안할 때\
어떤 기준으로 생각하고 판단해야 하는지를 정의한다.

---

## 1. 문서의 목적

CLAUDE.md는 다음을 위한 문서다.

- 코드 작성 방식의 일관성 유지
- 코드 변경 시 사고 기준 명확화
- 성급한 추측·부분 수정·과잉 추상화 방지

이 문서는 구현 설명서가 아니라 판단 규칙 문서다.

---

## 2. 프로젝트 설명 문서 (Source of Truth)

- 현재 이 프로젝트에 대한 공식 서비스 설명 문서는 아직 정의되어 있지 않다.
- 서비스 구조·도메인 개념·비즈니스 맥락에 대한 추측을 금지한다.
- 서비스 이해가 필요한 변경이 요구될 경우, 작업을 진행하기 전에 문서 정의가 선행되어야 한다.

---

## 3. 기본 사고 원칙 (Mindset)

- 시니어 엔지니어처럼 생각한다.
- 추측으로 결론을 내리지 않는다.
- 항상 최소 2가지 대안을 비교한 뒤 가장 단순한 해법을 선택한다.
- "지금이 아니라 나중에 깨질 코드"를 경계한다.

---

## 4. 필수 규칙 (Hard Rules)

다음 규칙은 예외 없이 반드시 지킨다.

- 코드를 수정하기 전에 관련 파일을 처음부터 끝까지 읽는다.
- 정의, 참조, 호출 경로를 파악하지 않고 수정하지 않는다.
- 비밀값(API Key, Token, Secret 등)을 코드·로그·문서에 남기지 않는다.
- 섣부른 추상화를 도입하지 않는다.
- 변경 전/후 영향도를 1~3줄로 명확히 인식한다.
- 수정 대상 파일뿐 아니라, 직접/간접 의존 파일도 함께 확인한다.

---

## 5. 코드 수정 전 체크리스트

- 이 코드는 어디서 호출되는가?
- 다른 레이어에 영향이 있는가?
- 테스트 또는 검증 코드가 필요한가?
- 지금 변경이 임시방편은 아닌가?

---

## 6. 코딩 컨벤션 (공통)

### 6.1 네이밍 케이스 규칙

- 변수, 함수: camelCase
- 클래스, 타입, 인터페이스: PascalCase
- 상수: UPPER_SNAKE_CASE

### 6.2 Boolean 네이밍 규칙

- is: 상태
- has: 소유
- can: 가능
- should: 조건
- will: 미래

### 6.3 함수 원칙

- 함수는 한 가지 책임만 가진다.
- Early Return을 우선한다.
- 매직 값 금지, 상수화.
- 입력 → 처리 → 반환 구조 유지.

---

## 7. 클린 코드 원칙

- 의도를 드러내는 이름을 사용한다.
- 가드절 우선.
- 실패는 명확히 드러낸다.

---

## 8. 안티 패턴

- 문맥 없는 부분 수정
- 근거 없는 최적화
- 과도한 추상화
- 광범위한 예외 처리
- TODO 방치

---

## 9. 코드 규칙 문서 (AI Context)

- 공통 규칙만 이 문서에 둔다.
- 프론트엔드, 백엔드, 인프라, 데이터 관련 코드 규칙은 `./.claude/` 디렉터리에 정의되어 있다.
- 역할별 규칙은 다음 문서를 기준으로 한다.
  - 프론트엔드: `.claude/frontend.md`
  - 백엔드: `.claude/backend.md`
  - 인프라/배포: `.claude/infrastructure.md`
  - 데이터/DB: `.claude/data.md`
- 해당 영역의 코드를 수정할 때는 반드시 관련 문서를 먼저 읽고 따른다.

---

## 10. 최종 원칙

이 규칙이 없으면 더 나쁜 결정을 하게 되는가?\
Yes일 때만 추가한다.
